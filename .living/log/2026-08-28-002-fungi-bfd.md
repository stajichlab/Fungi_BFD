---
session_id: 2026-08-28-002
project: fungi-bfd
branch: "main"
started: 2026-08-28
ended:
duration_minutes:
files_changed:
---

## Session Log — paralogoscope (wgd) pipeline validated on real data + BFD hash-bucket output layout

### What was done

1. **Real-data smoke test** (in `$SCRATCH`, config `plg_test.config`, work `work_r3`):
   - Root-caused r1 failure: Nextflow stages inputs as symlinks into workdir
     pointing at `/bigdata/...` while the wgd SIF only bound
     `${PWD},${projectDir},$TMPDIR`. Fix: bind `/bigdata:/bigdata,/rhome:/rhome`
     in all 3 WGD modules.
   - Fixed Biopython shadowing: container resolved host
     `/rhome/jstajich/.local/lib/python3.9/site-packages/Bio` first
     (`Bio.Alphabet` removed). Fix: `--env PYTHONNOUSERSITE=1`.
   - WGD_DMD 2/2 exit 0 on real genomes, published to
     `out_r3/{id}/wgd_dmd/{id}.cds-transcripts.fa.tsv`. WGD_KSD running (slow;
     login node capped at 4 cpus / 24 GB). Let finish per user; long steps go
     to SLURM.
2. **Queue → preempt** (user directive): `profile_paralogoscope.config` now
   `queue='preempt'`, `-A preempt -p preempt`.
3. **BFD hash-bucket storeDir layout** implemented in WGD_DMD / WGD_KSD /
   WGD_SYN: `storeDir { "${params.outdir}/wgd_{dmd|ksd|syn}/${hashBucketForType('wgd_dmd', meta.locustag)}" }`;
   outputs moved to workdir root (no double-nesting); natural wgd filenames
   kept (avoids storeDir staleness on re-annotation). Include path
   `../../../common/utils.nf` (3-level). Stub-validated 6/6 clean.
4. **Docs**: README.md + CHANGES.md document the new `paralogoscope` pipeline;
   `.living/` updated (decisions, learnings, this log).

### Decisions

- storeDir (BFD convention) vs publishDir; bucket keyed on stable LOCUSTAG;
  natural filenames preserved. See `.living/decisions.md` D-entry
  "WGD modules publish through BFD hash-bucket storeDir layout (2026-08-28)".

### Key outputs

- WGD modules: `nextflow/modules/comparative/wgd/{WGD_DMD,WGD_KSD,WGD_SYN}/main.nf`
- Launcher: `nextflow/run_paralogoscope.sh`; profile: `nextflow/conf/profile_paralogoscope.config`
- Test artifacts under `/scratch/jstajich/27843486/` (not committed)

### Next steps

- Let r3 ksd finish (real-data end-to-end validation).
- Full run via SLURM: `sbatch nextflow/run_paralogoscope.sh --wgd_sif ...`
  (function_neurospora inputs, ~4,365 genomes).
