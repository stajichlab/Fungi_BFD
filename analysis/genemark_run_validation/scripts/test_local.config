// Local-executor override for the GENEMARK_RUN standalone smoke test --
// the module's own cpus=16/memory=32GB/time=4h are production SLURM sizing;
// this session's interactive node only has 8 cores.
process {
    executor = 'local'
    withName: 'GENEMARK_RUN' { cpus = 8; memory = '16 GB' }
}
