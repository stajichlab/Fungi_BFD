# TODO Registry

> Master list of future work items. Add a detailed `todo/[item].md` writeup for complex items.

| ID | Item | Priority | Status | Added | Notes |
|----|------|----------|--------|-------|-------|
| T-016 | Extract UCR HPCC GPU node compatibility exclusions (K80/Blackwell6000, unsupported by current torch builds) out of per-pipeline `clusterOptions` (`profile_BFD.config`'s `idp` label, likely `profile_funannotate.config`'s `signalp` label) into a shared cluster-topology config, so the exclusion list isn't duplicated and drifting | medium | idea | 2026-08-01 | Short-term fix already applied: `--exclude=gpu01,gpu02,gpu03,gpu13,gpu14` added directly to `idp` label's `clusterOptions`. See `todo/ucr_hpcc_gpu_node_compat_config.md` for full context (found while debugging a `RUN_IDP`/aiupred CUDA failure in `compare/Exophiala`). |
| T-017 | Add protection/investigation for samples with no resolvable phylum: `GENOME_CLEAN`/`GENOME_CLEAN_BATCH` (`modules/funannotate/genome/`) hard-`exit 1` the whole clean task when `taxonkit lineage`/`reformat` can't resolve a numeric phylum taxid from `NCBI_TAXONID`, instead of flagging just that one genome (e.g. via `TO_ADD_TO_SUPRESS.csv`, same mechanism already used for too-short assemblies) and continuing the batch | medium | idea | 2026-08-04 | Also worth auditing `samples.csv` itself: 4 rows currently have an empty `PHYLUM` column (`GCA_023347065.1_ASM2334706v1` "fungal sp." concoct_113; `GCA_054392365.1_ASM5439236v1` "fungal sp. IPV F-433"; `GCA_058369715.1_StYLS` "Hypocreales sp. StYLS"; `GCA_024196135.1_ASM2419613v1` "Pleurotus ostreatus-Saccharomyces cerevisiae fusant" D1-P) — these are unresolved/composite taxa where a hard exit is especially likely. Found while investigating BFD run warnings (`params.suppress` default, `env` directive, `file()` glob) in the same session. |
| T-001 | Re-run `CALC_ASM_STATS` over current `samples.csv` so `results/genome_stats/asm_stats/` + `asm_stats.tsv.gz` cover the full annotated set (currently stale: 4,094 rows, missing all recent genomes) | high | pending | 2026-06-25 | `sbatch nextflow/run_functional.sh --run_pfam false --run_cazy false --run_merops false --run_signalp false --run_tmhmm false --run_targetp false --run_idp false --run_wolfpsort false --run_predgpi false --run_busco_genome false --run_busco_pep false` (run_asm_stats stays true). User to launch. |
| T-002 | Validate the too-small guard end-to-end: after T-001, confirm the merged `asm_stats.tsv.gz` flags the same 9 too-few-models genomes and no complete genomes; confirm guard skips populate `genome_annotation/predict_skipped_too_small.tsv` on next funannotate run | medium | pending | 2026-06-25 | See `analysis/funannotate_model_failures/` |
| T-003 | Consider separate handling for the oversized-genome `INCOMPLETE_unknown` failure class (0.5–1.3 Gb rusts; good 18–34 Mb assemblies timing out) — longer walltime / masking, not a size filter | low | pending | 2026-06-25 | e.g. `GCA_025201825.1_Phapa1` already in suppress.txt as "Too big - need to mask" |
| T-004 | Implement: share trained ab-initio gene-prediction parameters (AUGUSTUS/SNAP/GeneMark-ES) across ANI-qualified strains of the same species in `FUNANNOTATE_PREDICT`, instead of retraining per strain | high | core-wiring-done | 2026-07-24 | See `todo/species_level_abinitio_reuse.md` §8. Core `funannotate.nf` wiring implemented (§4.3, the "already-backfilled" path): `share_abinitio_params`/`abinitio_reuse_csv`/`force_independent_species` params, eager `loadAbinitioReuseMap()`, `sharedParamsJsonFor()`/`staleSharedParams()` helpers, `-p <parameters.json>` added to `FUNANNOTATE_PREDICT`'s command, provenance marker (`${out}.predict.abinitio_reused`), staleness extended to shared-params mtime. Verified via `-preview` (parses cleanly, both `share_abinitio_params=false` default and `=true` with a real 2-row test CSV — "Loaded 2 ab-initio reuse assignments... 1 reuse_eligible") and a standalone bash check of the conditional `-p` flag construction. **Not yet implemented**: the "in-run" join path (representative predicted fresh within the same run, not from a prior backfill — deferred, not needed for the two current pilots) and the first-N `BUSCO_PEP` integrated gate (§4.4, T-010). Decision logged in `.living/decisions.md`. |
| T-005 | Write `species_reuse_clusters.py` (`nextflow/bin/`) and backfill `_shared_abinitio/Aspergillus_fumigatus/` from the existing legacy PREDICT run — first concrete implementation step for T-004 | high | done | 2026-07-23 | `nextflow/bin/species_reuse_clusters.py` written + tested. Representative: `Aspergillus_fumigatus_Z5` (BUSCO 99.3%, chosen from ANI-covered candidates only — see bugfix note below). 373/375 non-representative strains reuse_eligible (ANI 99.14-99.93%), 2 fail-closed (no ANI data). Store backfilled at `genome_annotation/_shared_abinitio/Aspergillus_fumigatus/`; assignments at `genome_annotation/_reuse_assignments/abinitio_reuse_assignments.Aspergillus_fumigatus.csv`. Bugfix during testing: naive highest-BUSCO representative selection picked a genome with zero ani.db coverage (added after the last ANI run), orphaning the whole cluster — fixed by restricting candidates to those with >=1 ANI pair before ranking. |
| T-006 | Run a SPECIES-scoped ANI comparison for Beauveria (genus not yet in `ani.db`), then backfill `_shared_abinitio/Beauveria_bassiana/` — second pilot species for T-004 | medium | done | 2026-07-24 | Confirmed production `ani.db` now covers 204 distinct genera (T-009 fix validated in production) with 24,753 Beauveria bassiana within-species pairs. Backfilled `genome_annotation/_shared_abinitio/Beauveria_bassiana/` from representative `Beauveria_bassiana_KW2`. **Notable finding**: only 32/223 (14%) non-representative strains are `reuse_eligible` — median ANI to representative is 97.16% (well below Aspergillus fumigatus's 99.62%), consistent with *B. bassiana* being a known species complex in the literature. The 99% ANI gate correctly kept the other 190 strains on independent training rather than treating the species label as sufficient. Assignments at `genome_annotation/_reuse_assignments/abinitio_reuse_assignments.Beauveria_bassiana.csv`. |
| T-007 | Audit remaining `run_*.sh` launchers under `nextflow/` for the same missing-`module-load-singularity` gap found in `do_ANI/run_ANI.sh` | low | partially-done | 2026-07-23 | `do_ANI/run_ANI.sh` and `nextflow/run_ANI.sh` (top-level, symlinked from `do_annotation/nextflow/`) both fixed — both now `module load singularity` after `module load nextflow`. Not yet checked: `run_clean_genome.sh`, `run_comparative.sh`, `run_earlgrey.sh`, `run_funannotate.sh`, `run_functional*.sh`, `run_phyling.sh`, `run_seqstats.sh`, `run_test.sh`, `rescue_earlgrey_partials.sh` — only relevant for whichever ones use a profile with `singularity.enabled = true`. See `.living/learnings.md` "do_ANI/run_ANI.sh never loads the singularity module" (2026-07-23). |
| T-008 | Fixed: `SKANI_COMPARE`/`MASH_COMPARE`/`SOURMASH_COMPARE`/`FASTANI_COMPARE` (`compare_ANI.nf`) and `SKANI_DIST_QUERY` (`query_ANI.nf`) used `storeDir`, which never re-runs a group's ANI comparison after new genomes are later added to that group (found via `Aspergillus_fumigatus_F2`, added 2026-07-19, still missing from `ani.db` after multiple `-resume` reruns since) | high | done | 2026-07-23 | Switched all five to `publishDir { "..." }, mode: 'copy'` (closure syntax required for per-task-variable paths) so Nextflow's normal input-hash caching re-detects a changed group membership. Confirmed working: after manually deleting the stale `Aspergillus.full.ani.tsv`, the next run correctly recomputed it fresh (1,541 genomes). This specific fix is validated — but see T-009 for a separate, deeper problem discovered downstream of it. |
| T-009 | Fixed: `COMBINE_ANI_TABLE`'s merged `ani.db`/`all_pairs.csv` only reflected whichever group(s) were active in the latest run's live channel, not the full historical set across all ~248 genus groups — confirmed after T-008's fix: a run that correctly recomputed Aspergillus produced an `ani.db` with `COUNT(DISTINCT query_genus)=1`, silently dropping Beauveria's already-computed data | high | done | 2026-07-24 | `compare_ANI.nf`: `COMBINE_ANI_TABLE` now takes `--ani-manifest`/`--names-manifest` built by unioning a disk glob (`gatedGlobIn`, full historical coverage) with the live channel directly (`ani_tsv_ch`/`REPORT_ANI.out.names`, immediate + race-free for this run's own groups — needed because `publishDir`'s copy is asynchronous and can race the glob, discovered when a first attempt at the glob-only version silently never invoked `COMBINE_ANI_TABLE` at all). `combine_ani_table.py` rewritten to read manifests instead of directory globs, with `dedupe_by_group()` keeping the freshest (max-mtime) entry when a group appears via both sources. Verified: standalone script tests (basic + duplicate-group dedup) and a real `-stub-run --n_test 3 --taxon GENUS:Beauveria` end-to-end test (`completed=11`, includes `COMBINE_ANI_TABLE`, vs `completed=10` missing it before the union fix). See `.living/learnings.md` (2026-07-23/24 entries) for full history. **Unblocks T-006.** |
| T-010 | Build the first-N `BUSCO_PEP` integrated gate (§4.4 of the plan) — schedule representative + first N `reuse_eligible` strains, run existing `BUSCO_PEP` (`BFD.nf:961`, dedup'd via its own `storeDir`), gate the rest of that species' cluster on a completeness threshold before scheduling | medium | pending | 2026-07-24 | Cross-file integration (BFD.nf's `BUSCO_PEP` + `funannotate.nf`'s predict branching) — deferred out of the initial `funannotate.nf` wiring pass (T-004) to keep that change reviewable. This is the automated backstop from decision 9 (`.living/decisions.md`) against a bad representative silently propagating across a whole cluster. |
| T-011 | Implement the "in-run" join path for ab-initio reuse (§4.3) — a representative predicted fresh within the *same* pipeline run, not from a prior `species_reuse_clusters.py` backfill; requires an explicit dependency (representative's `.predict.done` marker per species) plus building/refreshing the shared store automatically right after the representative's PREDICT completes | low | pending | 2026-07-24 | Not needed for the two current pilots (Aspergillus fumigatus, Beauveria bassiana), whose representatives were already predicted in past runs — deferred until a genuinely new (never-predicted) species needs reuse enabled from its very first pipeline run. |
| T-012 | Fixed (two parts) and validated: AUGUSTUS requires (a) a species directory's basename to match its parameter-file prefixes, and (b) `*_parameters.cfg`'s own internal content (weightMatrixFile/infile/outfile directives) to reference the new prefix too — renaming files alone left stale internal references that crash-looped a validation run for 4+ hours (only visible in `logfiles/augustus-parallel.log`, not the main predict log) | high | done | 2026-07-24 | Part (a): relocated shared store to top-level `gene_prediction_shared_abinitio/<species_tag>/`, files renamed to `<species_tag_lower>_*`, symlinked into `AUGUSTUS_CONFIG_PATH/species/<name>/`. Part (b): `species_reuse_clusters.py` now text-substitutes the old prefix for the new one inside `.cfg` file content (not just filenames) when copying. **Validated end-to-end**: `Aspergillus_fumigatus_47-10` re-ran clean (verified-clean state, `stajichlab` queue) — zero crash-loop errors, reuse confirmed via marker, BUSCO 97.2% (vs legacy 96.9%, vs first broken-store attempt's 95.0%), 9,613 genes (vs legacy 9,878, vs broken-store's 9,078). Fix fully resolves the quality gap; reuse now matches or slightly exceeds independent training. See `.living/learnings.md` two entries (2026-07-24) for full incident history. |
| T-014 | Reorganize `results/genome_stats/*` (8 flat subfolders, up to 55,398 files) and `results/function/*` (9 flat subfolders) into hash-bucketed subdirectories keyed by ASMID/LOCUSTAG instead of one flat directory per type; convert `tables/*.csv.gz` staging to Parquet | high | **done** | 2026-08-01 | **All steps complete (2026-08-01).** Step 4 (#10, symlink-tree generator, PR #26) and step 5 (Parquet staging, split into #27/11a and #28/11b, PRs #29/#31) merged, completing the plan. #27: all 15 `MERGE_*` modules emit Parquet + master-only `MERGE_SAMPLES` (retires per-taxon subsetting, T-014 §D.2). #28: `build_BFD_duckDB.sh` rewritten to read Parquet into a single master `db/BFD.duckdb`. Both verified end-to-end against real production data (full-featured real BFD run for one genome, all 24 table types, real `duckdb` build + spot-checked join query). Follow-up `EXTRACT_BFD_TAXONOMIC_SUBSET` tool (post-hoc taxon-scoped DB extraction from the master, per §D.2) also implemented and merged — `scripts/extract_bfd_taxonomic_subset.py`, verified against real data (`GENUS:Malassezia`, 81 genomes). See `.living/decisions.md` for both the master-only-merge and the extraction-tool design decisions. Prior progress (steps 1-3, migration, asm_reports pruning) unchanged below. | **#19/#20/#21 all merged (2026-07-31)** — the live pipeline now fully reads/writes the bucketed layout: per-genome producer storeDir+filenames (#19, PR #23), batched BATCH_AA_FREQ/BATCH_CODON_FREQ per-genome bucket routing (#20, PR #24), full-rebuild glob patterns fixed (#21, PR #25 — confirmed via `-stub-run` that the old flat glob found 0 files where the new recursive one correctly found both test genomes'). Zero-recompute confirmed end-to-end (#19+#20 together: a second `-stub-run` with no cleanup dropped from 42→14 processes, every producer skipped via `storeDir`). **Remaining for T-014**: #10 (symlink-tree generator, unblocked) and #11 (Parquet staging, independent) — see §E. Historical context (asm_reports pruning, basename-ambiguity fix, O(#candidates) performance bug, the migration itself) preserved below. | Full plan, expert-reviewed (Fable, HPC/data-management persona) at `todo/genome_stats_storage_reorg.md`. Naming: ASMID key for asm_stats/asm_reports/BUSCO_genome, LOCUSTAG for BUSCO_protein/aa_freq/codon_freq/gene_stats/intergenic_stats/function/*; 2-hex-char hash buckets for most types, 3-hex for gene_stats/intergenic_stats (7x file-per-genome multiplier). Rejected genus/-based grouping (skewed genus sizes, 329 samples.csv rows with no GENUS). PR #12 (hash-bucket helper), #13+#14 (migration script + basename-ambiguity fix — see collision finding below), #17 (clearer reporting + O(#candidates) matching bottleneck fix) all merged. **Production `--apply` migration completed 2026-07-30**: 171,646 files processed across all 16 relevant types (`cazy` intentionally excluded, different pre-existing per-genome-subdirectory layout), 0 errors, spot-checks passed — 169,274 bucketed, 644 `_UNMATCHED/` (no samples.csv match), 10 `_COLLISIONS/` (output-path duplicates), 1,718 `_AMBIGUOUS_BASENAME/` (basename maps to >1 distinct genome — mostly harmless no-strain-fallback noise, ~81 are real full-basename collisions requiring manual review, e.g. `Fusarium_graminearum_PH-1` and `Hanseniaspora_thailandica_ZIM_2325`, two independent NCBI depositions sharing an identical strain label). Run via `scripts/one-off/submit_reorg_genome_stats_apply.sh`, one job per type chained via `afterok`, `--constraint='ryzen|cascade'` (see `.living/learnings.md` 2026-07-30 entry — an unconstrained first attempt landed on an old `amd,abu_dhabi` node and combined with an O(#candidates) matching bug to stall for ~1h with zero progress; both fixed). **`asm_reports` pruned (2026-07-30)**: confirmed dead (no producer, no consumer anywhere in the current pipeline) during #9 scoping — deleted `results/genome_stats/asm_reports/` (22,412 files) outright and `git rm scripts/collect_asm_stats.py` rather than migrating/updating either; removed from `TYPE_CONFIG`/`BUCKET_WIDTH` tables. See `.living/decisions.md`. **Issue #9 closed, split into #19 (9a), #20 (9b), #21 (9c)** after scoping revealed materially different code shapes: #19 (per-genome producer modules + `planFreq`/`staleGuard` + consumer scripts — mechanical), #20 (`BATCH_AA_FREQ`/`BATCH_CODON_FREQ` — genuinely different code, batched multi-genome tasks can't route to different buckets via a single `storeDir`), #21 (full-rebuild glob patterns in `BFD_MERGE.nf`/`BFD_FUNCTIONAL.nf` — flat globs will silently miss bucketed files otherwise, same failure class as the documented gated-glob-vs-live-channel learnings). **Fable pre-implementation review (2026-07-30) caught a critical scoping gap before any code was written**: the completed migration didn't just relocate files, it *renamed* every LOCUSTAG-keyed one from `${meta.id}` to `${meta.locustag}` — every current producer module (12 of 13 checked) still emits `${meta.id}`-named output, so "just add a bucket to storeDir" would have caused mass silent recompute + permanent duplicate-filename orphans. #19/#20 rewritten to require both the storeDir bucket AND the filename fix, with a two-part acceptance test (zero-recompute + zero-duplicate-filename). See `.living/learnings.md` 2026-07-30 entry. **Next**: #19/#20/#21 (unblocked) and #10 (symlink-tree generator, unblocked) are the remaining steps before T-014 can be marked done; #11 (Parquet staging) remains independent. See §E for the original 5-PR delivery order. **Minor migration residue found during #19 implementation (2026-07-31, not blocking)**: 4/5,818 `BUSCO_protein` files retain a leftover suffix from an older strain-naming convention that no longer exactly matches current samples.csv (e.g. `F07B100A_CBS_101.48_substr._RVII-324_met-.BUSCO_summary...` instead of clean `F07B100A.BUSCO_summary...`) — the legacy migration's longest-prefix matcher correctly fell back to a shorter (no-strain) match when the full current-strain string didn't exactly align with the old filename's suffix (a delimiter mismatch, likely from historical strain-string drift). Affects `tables/BUSCO.csv.gz` ASMID values for these 4 genomes only. Low priority, rare (0.07%), not caused by or blocking #19/#20/#21 — a manual rename or a small follow-up script would fix it. |
| T-015 | Investigate PFAM/HMMER scan performance: scratch-copy the Pfam-A HMM DB and MPI task scaling | low | **fixed-default-unchanged** | 2026-08-01 | **Fully done (2026-08-01), PR #32 merged.** Scratch-copy: no benefit (compute-bound, ~1% noise) — not implemented. MPI: found and fixed two real bugs in `RUN_PFAM`'s previously-broken/dormant MPI code path — (1) `srun` needs `--cpu-bind=none` on this cluster (general HPCC finding, now in `~/.claude/skills/nextflow-hpcc/SKILL.md`), (2) `hmmsearch` rejects `--cpu`+`--mpi` together (only surfaced via real-pipeline testing, not the original raw-`sbatch` A/B). Re-validated end-to-end through the real Nextflow pipeline against a larger, more typical genome (10,079 proteins): **16.2% real speedup** at `pfam_tasks=4` (9m 9s → 7m 40s). Production default deliberately left at `pfam_tasks=1` — enabling MPI broadly is a separate rollout decision (queue/scheduling behavior at scale), not bundled into this correctness fix. See `todo/pfam_hmmer_performance.md` and `.living/learnings.md` (2026-08-01, two entries) for full detail. |
| T-013 | Done: 12-strain validation (6 Aspergillus fumigatus, 6 Beauveria bassiana) comparing T-013 (no-TRAIN + `--transcript_evidence` + `-p` ab-initio reuse) against T-004 (ab-initio reuse WITH `FUNANNOTATE_TRAIN`) | high | validated-not-default | 2026-07-25 | **Result**: BUSCO preserved within noise (mean Δ -0.13pp, median -0.05pp, range -1.0 to +0.5pp, n=12) but gene count consistently lower under T-013 (mean -442, 12/12 strains negative, 5-7% fewer models) — *A. fumigatus* loses more genes than *B. bassiana* (mean -572 vs -312) despite similar BUSCO deltas. Wall-clock: ~69% total reduction (TRAIN+PREDICT ~150min → PREDICT-only ~46min per strain), or 44.5% if isolating just the PREDICT-internal ab-initio-training-skip effect. 4/12 strains cross-checked against legacy independent-training baselines — T-004 (not T-013) tracks legacy within ±0.3pp BUSCO, confirming ab-initio reuse alone (T-004) doesn't degrade quality. Full data/analysis in `.living/findings/funannotate-abinitio-reuse-validation.md` (F-006). **Fix applied along the way**: shared `parameters.json` for both species now includes a `glimmerhmm` empty-stub entry (weight=0 doesn't skip glimmerhmm's *training*, only its EVM consensus contribution — without a stub it silently fell back to a full genome-mode BUSCO run); `species_reuse_clusters.py` writes this stub automatically for future species backfills. See `.living/learnings.md` 2026-07-25 entries (glimmerhmm gotcha + an OOM-kill/exit-status-masking bugfix in the batch driver scripts). **Recommendation**: T-004 is production-ready as-is; T-013 needs the gene-count delta characterized (which specific models are dropped — redundant/low-confidence vs. genuinely missed) before defaulting to it, despite the attractive BUSCO-neutral result and much larger wall-clock win. Motivated by `analysis/funannotate_train_stage_timing/`'s finding that PASA is median 84.8% of `FUNANNOTATE_TRAIN` wall time. |
| T-018 | `find_telomeres.py`'s default `--both-ends false` means only monomers with an explicit RC-symmetric twin in the pattern list (currently just `TTAGGG`/`CCCTAA`) can be found regardless of a contig's arbitrary assembly strand orientation; other default monomers (`TAA[C]+`, `TTAGG`, etc.) have no RC twin and can silently miss real telomeres at both ends of a "wrong-orientation" contig | medium | idea | 2026-08-04 | Flagged by bioinformatics-expert review during the 2026-08-04 3'-end bug fix (see `.living/decisions.md` "H"). Fix options: default `--both-ends` to true (doubles search volume/runtime pipeline-wide — needs a deliberate rollout decision, not a silent default flip), or ensure every default monomer has an explicit RC-symmetric twin the way `TTAGGG`/`CCCTAA` already does. |
| T-019 | Evaluate `find_telomeres.py`'s handling of incomplete/degraded repeat tracts (partial trailing monomer, single mismatched bases below `--fuzzy` thresholds, short tracts near `--min-repeats`/`--min-length` cutoffs) and assess whether a dedicated Tandem Repeat Finder approach (e.g. TRF, or a proper approximate-repeat aligner) is needed alongside/instead of the current regex+fuzzy scheme to strengthen recall on divergent/degenerate fungal telomeres | medium | idea | 2026-08-04 | Raised during the 2026-08-04 3'-end bug fix review — the regex/fuzzy modes were validated for exact-ish tandem runs, but recall on genuinely degraded terminal repeats (common in draft assemblies where telomere-adjacent bases are lower quality) is untested. Worth a comparison run against TRF or similar on a sample of the 5 test genomes (`GCA_009805915.1_ASM980591v1`, `GCA_982397285.1_T36-F`, `GCA_041684285.1_ASM4168428v1`, `GCA_018345885.1_ASM1834588v1`, `GCA_900073075.1_CML3066.v2`) to see whether it catches tracts `find_telomeres.py` misses. |
| T-020 | Consider generalizing/upstreaming `find_telomeres.py` into AAFTF as a reusable repeat-class detector (not telomere-specific), so other tandem-repeat classes (e.g. rDNA arrays, centromeric repeats) could reuse the same terminal/internal search machinery | low | idea | 2026-08-04 | Deferred until a table of repeats has actually been derived from complete fungal genome analyses across the BFD set (depends on T-019 and on running the now-fixed `find_telomeres.py` kingdom-wide) — premature to generalize the interface before real usage data shows what the general case needs to support. |
| T-021 | Develop a BFD_interpro offshoot — run InterProScan (`-goterms -pa`) over a representative-proteome subset (natural set: the SELECT_REPS species representatives, ~27 genomes) and merge outputs into per-protein `interpro.parquet` / `go.parquet` (keyed `protein_id`/`asmid`, mirroring the `MERGE_*` + duckdb pattern) so all 23k+ BFD gene records get rep-set GO/pathway/InterPro annotation via mapping. Do NOT make IPS the main BFD functional engine (23k+ genomes × IPS cost is orders of magnitude too heavy) | low | idea | 2026-08-10 | **Insight (design discussion 2026-08-10):** BFD's functional stack (`modules/BFD/`: PFAM, CAZy, MEROPS, SignalP, TMHMM, TargetP, IDP/AIUPred, WoLF PSORT, PREDGPI) overlaps InterProScan **only on the Pfam axis** — and BFD's `pfam.parquet` keeps alignment-level domain coords (`hmm/ali/env`) IPS's TSV lacks, so it's not redundant work. The real gap: BFD produces **no GO terms, no pathways, and none of IPS's other ~14 member DBs** (PANTHER, SUPERFAMILY, Gene3D, SMART, TIGRFAM, CDD, ProSite, Coils, HAMAP, MobiDBLite). Also note funannotate's pipeline already runs `INTERPROSCAN_RUN` (`modules/funannotate/function/`, wired at `FUNANNOTATE_ANNOTATION.nf:113`) → harvest `iprscan.xml`/GO from annotated genomes rather than re-run when available. Prior exploration already exists: `development/interproscan6.nf` (SLURM) + `development/interproscan6_k8s.nf` (Nautilus, IPS 6.0.0 docker ~8 GB image / ~80 GB data on PVC); HPCC has `interproscan/5.54–5.72` + `db-panther/13.1`. Config trickiness + the large cached PANTHER DB are the sizing friction — `-appl` can drop PANTHER (GO still derivable from the InterPro-entry map). Details to be sketched later. |
| T-022 | Wire GeneMark ET mode into `GENEMARK_RUN/main.nf` and `FUNANNOTATE_PREDICTION.nf` | medium | **done** | 2026-08-12 | **Closed 2026-08-12.** Recipe validated with a real `gmes_petap.pl --ET` run (10,776 gene models) after root-causing an initial failure by cross-checking BRAKER (`Gaius-Augustus/BRAKER` on GitHub): the fix was `bam2hints --intronsonly` → `filterIntronsFindStrand.pl` (canonical splice-site check + strand assignment, vendored at `nextflow/bin/vendor/filterIntronsFindStrand.pl`, Artistic License) → `join_mult_hints.pl` → `gmes_petap.pl --ET`, seeded by `training/transcript.alignments.bam` (already produced by `FUNANNOTATE_TRAIN`). Then wired for real: `GENEMARK_RUN/main.nf` gained a `mode`/`training_bam`-gated ET branch (falls back to ES when no RNA-seq), `genemark_mode` + a new `trainingTranscriptBamFor()` helper (`utils.nf`) threaded through all 3 `GENEMARK_RUN`/`GENEMARK_RUN_SIB` call sites in `FUNANNOTATE_PREDICTION.nf`. Real-smoke-tested against the actual wired module (`nextflow/genemark_run_smoke.nf`, deliberately placed at `nextflow/` top level — see the `workflow.projectDir` gotcha noted in the design doc): **10,780 gene models** (matches the standalone recipe's 10,776), plus a fast-reuse regression check confirming the ES/`--predict_with` path still works with the new 10-element input tuple. Full trace: `nextflow/docs/GENEMARK_RUN_DESIGN.md` "ET mode" section; `analysis/genemark_run_validation/GENEMARK_RUN_VALIDATION.md`. |
| T-023 | Fix `species_reuse_clusters.py`'s undeduplicated `backfill_species_store()` to accept an explicit `genemark_mod` path (like `backfill_abinitio_params.py`'s now does), or delete it in favor of the shared implementation | low | idea | 2026-08-12 | Found while wiring `GENEMARK_RUN`: `species_reuse_clusters.py` has its own separate, never-deduplicated copy of `backfill_species_store()` (different signature) that always derives `genemark.mod` from `predict_misc/ab_initio_parameters/` — a location GeneMark no longer writes to once a representative goes through `GENEMARK_RUN`. Only referenced from the inactive `nextflow/legacy/funannotate.nf`, so not urgent, but will silently backfill augustus+snap only (missing genemark, no crash) if run against a GENEMARK_RUN-era representative. See `nextflow/docs/GENEMARK_RUN_DESIGN.md` "Known gap" section. |
| T-024 | Test `ghcr.io/nextgenusfs/funannotate:v1.9.0-beta.4` against the packaging bugs found in beta.2/beta.3 | low | idea | 2026-08-12 | User (image maintainer) reports beta.4 is now released. Not yet re-checked whether it resolves the `fasta`→`fasta3`/`fasta36` symlink bug, the missing `LD_LIBRARY_PATH=/venv/lib` Dockerfile `ENV`, or the Debian `which` flag-mismatch — see `.living/learnings.md` (2026-08-12 container smoke-test entries) for the beta.2/beta.3 findings this should be re-validated against. |
| T-025 | Restructure `FUNANNOTATE_RNASEQ.nf`'s SRA/training param gating — design finalized via grilling session 2026-08-14, ready to implement | high | design-approved | 2026-08-14 | **Incident**: `run_sra_fetch=false` (intended to mean "skip network, use already-cached local reads") silently skipped training for ALL strains including ones with real RNA-seq on disk — no error, plausible `predict_results`, GeneMark fell back to ES for everyone. Fable's independent design review found 4 issues of the same silent-degradation shape; a 5th (more severe) was found during the follow-up grilling session. **Approved design (all 6 points confirmed with Jason 2026-08-14):** (1) Deprecate/remove `run_sra_fetch` — its 3 jobs (disk-check, network permission, "does training run") get split apart below. (2) Disk-check (`rnaseq_reads/${species_tag}_norm_*.fastq.gz` / cached `sra_query.csv`) becomes UNCONDITIONAL — always checked first, online or offline; training runs for any species that ends up with reads, from disk or network, never gated by a separate flag. (3) New flag `allow_sra_network` (bool, default `true`) uniformly gates both `SRA_QUERY_BATCH` and `SRA_FETCH`/`SRA_FETCH_SE` network calls; when `false`, a species with no cached data fails over to the same `no_data`/`WRITE_EMPTY_READS` path as a genuinely-RNA-seq-less species — never errors, never silently trains on nothing. (4) **Critical fix required as part of this**: today, under `skip_sra_query=true`, a species with no cached query CSV is `log.warn`'d and filtered out of `sra_query_results` entirely — since `assembly_with_reads = predict_genome_ch.combine(reads_ch, by:0)` is an inner-join, that species' genomes silently vanish from the ENTIRE run (not degraded — completely absent, no predict, no error). Must be fixed so cache-miss species route through `no_data`/`WRITE_EMPTY_READS` instead of being dropped from the join. (5) `skip_sra_query` KEPT as an independent online-mode knob (trust cached CSV, still allow fetching known accessions) — not folded into `allow_sra_network`. (6) Representative-strain selection (was: silent fallback to `outs[0]`) becomes a MANDATORY hard-error upfront (before any SLURM work submitted) for any multi-strain species missing an `abinitioReuseMap` entry — "run `compare_ani` first for: species X, Y, Z". Single-strain species are exempt entirely (no ambiguity, no ANI needed). (7) `storeDir` on `SRA_FETCH` is KEPT (normalization is expensive, "never redo by accident" is a legitimate goal) but gets an explicit `--force_refetch <species_tag>[,...]` escape hatch so blacklist edits have a real, documented way to take effect instead of silently no-op'ing. (8) `staleRnaseq()` gets a second, independent check — RNA-seq/Trinity mtime vs. `training/funannotate_train.pasa.gff3`'s own mtime, not gated on the predict GBK existing — OR'd with the existing GBK-based check. See `.living/learnings.md` (2026-08-13, "beta.5 real end-to-end confirm run") for the incident trace. Not yet implemented — this row is the spec to build against. |
| T-026 | Fix remaining silent-degradation findings in `FUNANNOTATE_PREDICTION.nf`/orchestration (Fable review 2, 2026-08-14) — `FUNANNOTATE_ANNOTATION.nf`'s matching bug already fixed same day | high | design-approved | 2026-08-14 | Follow-up Fable review of `FUNANNOTATE_GENOME_PREP.nf`/`FUNANNOTATE_PREDICTION.nf`/`FUNANNOTATE_ANNOTATION.nf`/`funannotate.nf` (same review lineage as T-025). **Already fixed 2026-08-14**: `FUNANNOTATE_ANNOTATION.nf`'s `combine(reads_ch, by:0)` inner-join silently dropped genomes with no reads_ch entry from BOTH UPDATE and ANNOTATE (not just UPDATE) — switched to `join(..., remainder:true)` + explicit `hasReads()` guard, no-reads species now `log.warn` (decided: warn only, not a report file — skipping UPDATE with no RNA-seq is routine at BFD scale, not exceptional) and still flow into ANNOTATE via a synthetic `upd_signal` entry. **Remaining, not yet implemented:** (1) `loadAbinitioReuseMap()` has no staleness check — a strain added to `samples.csv` after `compare_ani`/`busco_genome` last ran, or a genome replaced under the same asmid, is silently treated as a legitimate singleton (independent training, zero warning). **Decided fix (2026-08-14 grilling session)**: at `funannotate.nf` startup, compare each assigned species' genome file mtime against `abinitio_reuse_assignments.csv`'s own mtime (not the underlying parquet/TSVs individually) and **hard-error** (same severity as the existing "CSV entirely missing" check) naming exactly which species look stale — consistent with T-025 point 6's mandatory-ANI decision. (2) `--predict_scope representative_only` silently drops independent/singleton genomes with zero logging (by design for the two-pass workflow, but no visibility if the follow-up `all` pass is forgotten) — add a `log.info` count, ideally a report file mirroring `predict_blocked_awaiting_representative.tsv`. (3) A representative that's predicted but never backfilled (e.g. run interrupted between predict and backfill) permanently wedges its siblings — reported via the blocked-siblings TSV so not fully silent, but no self-healing; add `sharedParamsJsonFor(sp) == null` as an extra `rep_todo` trigger condition alongside the existing staleness checks. (4) Minor/cosmetic: `is_representative` classification ignores `forceIndependentSet`, causing one wasted `BACKFILL_ABINITIO_PARAMS` call for force-independent species' nominal representative — add `&& !forceIndependentSet.contains(sp)` to the `is_rep` computation. **Orchestration decision (confirmed 2026-08-14)**: keep the 3 separate pipeline invocations (`busco_genome` → `compare_ani` → `funannotate`) rather than folding ANI/BUSCO inline into `funannotate` — `storeDir` caching already makes reruns cheap, and the outputs are independently useful elsewhere; the staleness cross-check in (1) above is the actual fix needed, not a restructure. Sound, no issues found: `FUNANNOTATE_GENOME_PREP.nf` (proper `log.warn` on every drop), the blocked/gated sibling machinery in `FUNANNOTATE_PREDICTION.nf`, and the GeneMark ES/ET wiring. |
| T-027 | `*_odb12` BUSCO lineages in the shared FUNANNOTATE_DB were missing `lengths_cutoff` | low | **resolved** | 2026-08-14 | **Resolved same day**: Jason moved all extracted `*_odb12/` dirs out of `funannotate_db/`'s top level (into a new `odb12/` subfolder; original `.tar.gz` archives left in place) so nobody can accidentally select one via `BUSCO_LINEAGE` in the future. Original finding below. | Found while standalone-testing `funannotate predict`'s `-w genemark:1` weight override: I (the assistant) picked `--busco_db fungi_odb12`/`saccharomycetes_odb12` for the test myself, without checking what real `samples.csv` actually uses. BUSCO failed with `ERROR Impossible to read the lengths in .../lengths_cutoff`. Systematically checked every lineage dir: **all 5** `*_odb12` dirs (`fungi`, `microsporidia`, `mucorales`, `mucoromycota`, `saccharomycetes`) are missing `lengths_cutoff`; **all `*_odb10`** lineages (~150, checked every one) and all legacy-format ones (`dikarya`, `*_odb9`) have it. Root cause: funannotate v1.9 predict's ab-initio BUSCO step uses its own vendored old BUSCO2 script (`funannotate-BUSCO2.py`, only reads `lengths_cutoff`, never generates it) — incompatible with the newer OrthoDB12 dataset layout from `busco-data.ezlab.org`; `*_odb10` and legacy formats are fine. **Checked real `samples.csv`: `BUSCO_LINEAGE` values are ALL bare/unsuffixed** (`dikarya` 22,281 rows, `fungi` 549, `mucoromycota` 290, `microsporidia` 163) — each resolves to its own separate legacy-format directory (`dikarya/`, `fungi/`, `mucoromycota/`, `microsporidia/`, distinct from the `*_odb12/` dirs), **all four confirmed to have `lengths_cutoff` present**. So this gap has zero current production impact — logged only so nobody manually sets a `*_odb12`-suffixed `BUSCO_LINEAGE` in `samples.csv` in the future without knowing it won't work, and prefer `*_odb10` over `*_odb12` if a newer/different lineage is ever needed. |
| T-028 | Containerized `funannotate update` fails: PASA's `SAMPLE_HOOKS/` dir never copied by `install.sh`, breaking `GFF3::GFF3_annot_retriever` hook resolution | high | root-caused, fix identified | 2026-08-14 | **Not yet fixed** (external repo, not this one). Found during the first real (non-stub) containerized `funannotate update` run (`do_container_confirm_test/`, beta.5). `Load_Current_Gene_Annotations.dbi` unconditionally requires the `GFF3::GFF3_annot_retriever::get_annot_retriever` hook whenever its `-P` arg is a `.gff3` file (which `funannotate update` always passes) — dies with `Error, couldn't resolve path for GFF3::GFF3_annot_retriever`. This project's own `~/.pasa/pasa_conf/conf.txt` already correctly sets `HOOK_PERL_LIBS=__PASAHOME__/SAMPLE_HOOKS` (matches upstream `Docker/conf.txt`) -- not a config bug on our side. **Root cause, confirmed against the actual fork source** (`~/projects/funannotate/PASApipeline`, `git@github.com:hyphaltip/PASApipeline.git` branch `rust_optimize`): `scripts/install.sh` copies `PerlLib`, `pasa_conf`, `schema`, `scripts`, `pasa-plugins`, `misc_utilities` into the install prefix (~lines 160-195) but never copies the separate top-level `SAMPLE_HOOKS/` dir (`SAMPLE_HOOKS/GFF3/GFF3_annot_retriever.pm`, `SAMPLE_HOOKS/GTF/Gtf_annot_retriever.pm`) -- confirmed `/venv/opt/pasa/src/SAMPLE_HOOKS` is entirely absent in the built beta.5 image. **Fix**: add `if [ -d "${PASA_ROOT}/SAMPLE_HOOKS" ]; then cp -r "${PASA_ROOT}/SAMPLE_HOOKS" "${SRC_DIR}/" \|\| true; fi` to `install.sh` alongside the existing copy blocks (~line 162), then rebuild the `funannotate-live` image -- `pixi_install_pasa.sh` delegates entirely to `install.sh`, no Dockerfile change needed. Also fixes the identical latent `.gtf`-input case (`GTF::Gtf_annot_retriever`). See `.living/learnings.md` (2026-08-14, "beta.5 real end-to-end confirm run, take 2") for the full trace. Until fixed, keep `run_update`/`run_annotate`'s update leg off in production `params_confirm.yaml`-style configs. |
